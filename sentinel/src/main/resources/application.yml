server:
  port: 18081

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9010/eureka
      
#设置eureka服务的名称
spring:
  application:
    name: sentinel-hi

  ### 设置Sentinel控制台的主机地址和端口
  cloud:
    sentinel:
      transport:
        dashboard: localhost:9000